<nav>
    <div class = "nav_div">
        <a class= "nav_a" href="/">Acasa</a> 
        |
        <a class= "nav_a" href="/despre">Despre</a> 
        |
        <a class= "nav_a" href="/contact">Contact</a> 
        |
        <% if(creatDB) {%>
            <a class= "nav_a" href="/meniu">Meniu</a> 
        <% } else { %>
            <a class= "nav_a" href="/creare-db">Meniu</a>
        <% } %>
        </div>
</nav>
<% if (username)  {%>
    <p id="conectat">Sunteti conectat ca <%=fullname%>. 
        <a href="/deconectare">Deconectati-va.</a></p>
    <% } else {%>
        <p>Nu esti autentificat. <a href="/autentificare">Autentifica-te!</a> </p>
    <% } %>

<h1 class = "cos">Cosul dumneavoastra: </h1>

<% var locatie_poza_1 = "../images/Menu/"; var locatie_poza = ""; %>
<% if (cosDB) {%>
<div id = "afisare_cos_db">
<table>
    <tr>
        <th></th>
        <th>ID</th>
        <th>Nume preparat</th>
        <th>Ingrediente</th>
        <th>Gramaj (g)</th>
        <th>Pret (lei)</th>
    </tr>
    <%for(var i=0; i<cosDB.length; i++) { %>
        <tr>
            <% locatie_poza = locatie_poza_1 + cosDB[i][0] + ".jpg"%>
            <td><img width = 100 height = 100 src = <%=locatie_poza%>></td>
            <td><%=cosDB[i][0]%></td> 
            <td><%=cosDB[i][1]%></td> 
            <td><%=cosDB[i][2]%></td> 
            <td><%=cosDB[i][3]%></td>
            <td><%=cosDB[i][4]%></td>  
        </tr>
    <% } %>
    </table>
    <div id="cos_form_div">
    <form id = "cos_comanda" action="comanda" method="POST">
        <input type="submit" value="Comanda">
    </form>
    </div>
</div>
<% } %>
